# Backend the packaged Android app talks to.
# A phone cannot reach "localhost", so this must be the deployed backend URL.
# Change this if your production backend runs elsewhere.
VITE_API_BASE_URL=https://payrollback.systimus.in
