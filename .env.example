# Leave empty to use the production same-origin Nginx proxy.
VITE_API_BASE_URL=

# Runtime Nginx upstream. In Dokploy this can be a private service URL or
# the public HTTPS backend domain.
BACKEND_UPSTREAM=https://api.payroll.example.com
